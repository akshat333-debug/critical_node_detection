# Legacy Streamlit Architecture

This folder contains the original `app.py` script developed during Phase 1 of the project. It has been retained for archival purposes.

The project has since been decoupled into a FastAPI backend (`api/main.py`) and a Vite React frontend (`frontend/src/`) which serves as the active single source of truth.
